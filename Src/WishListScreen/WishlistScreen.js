import React from "react";
import { View, Text, StyleSheet } from "react-native";

const WishlistScreen = () => {
    return (
        <View>
            <Text style={{ color: '#000' }}>This is Wishlist Screen</Text>
        </View>

    );
};

const styles = StyleSheet.create({});

export default WishlistScreen;